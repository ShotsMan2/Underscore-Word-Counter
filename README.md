# Underscore-Word-Counter
A C program that parses a string input where words are separated by underscores ('_') and calculates the total word count without using standard string library functions like strlen.
